// User-visible delays for phase transitions. Held views before returning to MENU /
// PLAYING. Replace with cinematic-driven timing in Phase 5/6.
export const TRANSFER_DELAY_MS = 1000;
export const WIN_LOSS_DELAY_MS = 2000;
