# Copy to .env (gitignored) and fill in. Consumed by utils/deploy.sh.
# .env must never be committed: it names the deployment host and login.
DEPLOY_REMOTE=user@host.example
DEPLOY_REMOTE_DIR=~/sentinel
DEPLOY_IMAGE=sentinel
